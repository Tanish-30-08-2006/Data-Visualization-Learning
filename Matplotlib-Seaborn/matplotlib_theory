

MATPLOTLIB 


MULTIPLE PLOTS .................
SUBPLOTS --> multiple graphs to print in output next to each other never overlapping 
             (2,2,1) means 4 graphs can be plotted in z format indexing 

USING FIGURE --> Figure creates a canvas where by coordinates we can show a plot so in a large plot we can plot another smaller 
                 smaller plot on top right of smaller size so in 1 canvas multiple plots 

                 Function --> fig  = plt.figure(figsize=(10,8),dp1=100)   [ dpi is depth per inch to create clearer images nd pixels]
                              fig.add_axes( width and length of canvas)
                 
                 multiple axes can be created in a figure 



